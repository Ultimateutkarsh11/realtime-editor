// All events are listed here:
// A constant object called ACTIONS is defined containing key-value pairs representing different event types.

const ACTIONS = {
  JOIN: 'join',
  JOINED: 'joined',
  DISCONNECTED: 'disconnected',
  CODE_CHANGE: 'code-change',
  SYNC_CODE: 'sync-code',
  LEAVE: 'leave',
};

module.exports = ACTIONS;